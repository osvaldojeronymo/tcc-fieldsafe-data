
[project]
name = "tcc-fieldsafe-rgb-ensemble"
version = "0.1.0"
description = "Fusão multimodel supervisionada (RGB) no FieldSAFE."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src"]
